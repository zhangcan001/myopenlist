# 更新日志

## ✨ 新功能

- Windows 现支持为每个挂载单独设置“挂载为网络驱动器”；旧配置中的 `--network-mode` 参数会自动迁移，并保留其他额外参数。
- Windows 挂载常用参数中新增卷名（`--volname`）预设，便于自定义资源管理器中显示的驱动器名称。

## 🐛 修复的错误

- 默认启用 rclone 的 VFS 写入缓存，提高 WebDAV 挂载的文件写入可靠性；已手动设置缓存模式的配置不会被覆盖。
- 修复旧版配置缺少或包含空 `mount_config` 时，挂载管理页面可能空白的问题。
- 修复启用 HTTPS 的 OpenList Core 无法被正确监测、无法正确打开管理页面的问题；现支持本地自签名证书。
- 修复 Windows 防火墙状态检测依赖本地化 `netsh` 输出导致状态不准确的问题，并改善操作失败提示。
- 自动挂载远程 WebDAV 时不再无条件启动本地 OpenList Core，仅本地回环地址的挂载会启动核心。
- 改善 macOS 的 FUSE 库路径兼容性；同时修复 Windows 网络路径打开、Linux 更新包格式识别及 macOS 单实例监听相关兼容性问题。
